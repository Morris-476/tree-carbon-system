# 簡易固碳繪測（/measure）功能 — 重點整理

> 這份文件整理「簡易固碳繪測」頁面相關的所有背景、決策與現況，給下一個接手的 AI／開發者快速上手用。
> 撰寫時間：2026/09/05，撰寫當下的實際檔案狀態以 `git log`/程式碼為準。

---

## 1. 這功能是什麼、從哪來的

- 校內另有一個獨立專題「Tree-Trunk-Measurement」（路徑：`114學年度生活試驗室競賽/Tree-Trunk-Measurement`，**不是這個 repo，是同一台電腦上的另一個 git repo**），做的是「上傳一張樹幹照片＋手機型號＋拍攝距離＋樹種 → 算出樹徑（DBH）與固碳量」的獨立小工具，原本跑在 port 5001。
- 目前把它的**前端**複製進本專案，變成 `/measure` 頁面（導覽列「簡易固碳繪測」）。**後端運算邏輯正在搬、還沒搬完**。

### 鐵律（一定要遵守）

1. **Tree-Trunk-Measurement 原始專案完全不能動**——只能「複製」過來，不能「搬移」，原始檔案要維持原樣。之前不小心直接改過它的 `static/styles.css`，後來用 `git checkout` 復原過。
2. **不改變網頁架構既有邏輯**——`services/db.py`、`routes/admin.py`、`routes/api.py`、`routes/pages.py` 既有的東西不動，新功能都是新增檔案／新增函式。
3. 遵守本專案 `CONTRIBUTING.md` 的規範：Python 4 格縮排、字串用單引號、每個新檔案開頭要有 `# 負責人：` `# 開發日期：` `# 用途：` 三行註解、只在必要處加註解（不要每行都加）。

---

## 2. 目前檔案結構（已存在於 main）

```
templates/measure.html          頁面（extends base.html，套用共用導覽列，不是原本獨立的 <html>）
static/measure/app.js           前端邏輯，原封不動複製，完全沒改
static/measure/styles.css       樣式：白底＋黑框線，功能沒動，只改外觀
routes/pages.py                 加了 /measure 路由（GET，公開不需登入）
templates/base.html             導覽列「簡易固碳繪測」已連到 /measure

services/carbon.py               【空檔案，待實作】共用固碳／樹徑公式（FocalCalculator + CarbonCalculator）
services/db.py                   加了 get_species_list()（查 Species_Ref，純查詢無計算）

services/measure/                只給 /measure 頁面用，不共用給主站上傳管線
├── __init__.py                  空檔案
├── trunk_detector.py            單張照片版 YOLO 樹幹偵測（跟 services/analysis/tracker.py 的影片版不同，可共用同一份 best.pt）
├── geometry.py                  IQR 樹徑量測（20 切片＋四分位距過濾異常值，胸高 1.3m）
├── visualizer.py                標注圖繪製（遮罩、量測線、狀態標籤）；紅色量測線 thickness 已從 2 改成 10（*5，使用者要求）
├── models.py                    MeasurementResult 資料結構（量測結果統一格式）
├── validator.py                 簡化版：因為沒搬 QR code 比例尺（qr_detector.py/qr_calculator.py 完全沒搬），
│                                  只保留「焦距公式」這一支邏輯，沒有留「兩種方法比較差異」的死代碼
└── pipeline.py                  【空檔案，待實作】串接上面所有模組的主流程
```

---

## 3. 視覺設計決策歷程（已完成，不要再改，除非使用者要求）

- 導覽列：**全站**套用淺綠漸層 `linear-gradient(135deg, #a8e6c3, #4caf7d)`，寫在 `static/css/style.css` 的 `.navbar`（全站共用，不是只有 /measure 頁面），這樣所有頁面導覽列都一致。
- `/measure` 頁面本身（導覽列以下）：白底 + 黑色框線（`border: 1px solid #000`），所有原本的漸層／陰影都拿掉了。
- 首頁 `.hero-section` 灰色區塊：中途一度被改成綠色，後來**已改回原本的 `#d9d9d9` 灰色**（這是唯一一個「改了又還原」的地方，注意不要搞混）。
- `static/measure/styles.css` 目前完全沒有 `gradient` 或 `box-shadow`（都被拿掉了，只保留頁面自己 body 的白底）。

---

## 4. 關鍵決策記錄（Q&A 摘要）

以下是跟使用者討論後拍板的方向，之後不要重新問一次：

| 問題 | 決定 |
|---|---|
| /measure 頁面要不要接資料庫寫入（存進 Trees/Measurements）？ | **不用**，現在只是算完顯示結果，不寫入任何資料表 |
| /measure 要不要登入才能用？ | **不用**，公開頁面，`routes/pages.py` 沒加 `login_required` |
| 樹種資料要不要建表／匯入？ | 樹種係數（`allo_param_a`/`allo_param_b`）已經在 `Species_Ref`，還缺一個 `carbon_fraction` 欄位。**要把 Tree-Trunk-Measurement 的 16 筆樹種資料匯進 `Species_Ref`**，之後 /measure 跟主站共用同一份樹種資料。**這步需要 Azure SQL 連線才能執行，目前連不上，卡住**。 |
| 手機型號要不要建 DB 表？ | **/measure 本身不需要**——前端 `app.js` 裡已經寫死 `CAMERA_PRESETS`（各手機的 focal_mm/sensor_width），送出時直接把數值帶給後端，後端不用查表。DB 表只有「主站上傳管線也要做樹徑換算」時才需要，屬於未來另一件事，跟 /measure 現在能不能動無關。 |
| YOLO 模型檔案要不要另外準備？ | 不用，`/measure` 跟主站共用同一份 `Tree-Trunk-Segmentation/best.pt`（`.env` 的 `MODEL_PATH`），以後要換模型直接覆蓋檔案即可，不用改程式碼。 |
| 固碳公式（×3.67 CO2 當量）哪裡放？ | 放共用模組 `services/carbon.py`，因為主站上傳管線以後也需要同一套公式，不要兩邊各寫一份 |
| 哪些邏輯算共用、哪些算 /measure 專屬？ | 共用：焦距換算＋固碳公式（`services/carbon.py`）。專屬：單張照片的 YOLO 偵測、IQR 樹徑量測、標注圖繪製（`services/measure/` 資料夾），因為影片追蹤流程用不到這些 |
| `CarbonEquivalents`（生活化比較，如「相當於開車幾公里」）要做嗎？ | **不用，已刪除**——確認過 `static/measure/app.js` 前端根本沒有讀取／顯示這個欄位，做了也是死代碼 |
| `Validator` 要不要保留「QR vs 焦距公式」比較邏輯？ | **不保留**，因為 QR 相關檔案完全沒搬進來，`result_a` 永遠是 `None`，比較邏輯永遠不會執行到，所以簡化成只有「焦距公式」這一支 |
| 紅色量測線粗度？ | 使用者要求 *5，已從 `thickness=2` 改成 `thickness=10` |

---

## 5. 已知阻塞點

- **Azure SQL 連線目前連不上**（先前確認過，原因待查：可能是防火牆沒開放目前 IP、ODBC Driver 沒裝、或帳密問題）。這會卡住：
  - `Species_Ref` 加 `carbon_fraction` 欄位（DDL）
  - 把 16 筆樹種資料匯入 `Species_Ref`
  - `get_species_list()` 實際測試（函式已經寫好，但沒連線測不了）

---

## 6. 還沒做的事（照建議順序）

1. `services/carbon.py`——實作 `FocalCalculator`（像素寬＋距離＋焦距→樹徑）與 `CarbonCalculator`（`biomass = a×dbh^b` → `carbon = biomass×carbon_fraction` → `co2 = carbon×3.67`），可獨立完成，不需要資料庫連線
2. `services/measure/pipeline.py`——串接 `trunk_detector.py` → `geometry.py` → `services/carbon.py` → `validator.py` → `visualizer.py`，對外提供一個入口函式
3. `routes/measure.py`（或加進 `routes/pages.py`）——新增兩支 API：
   - `GET /api/species`：呼叫 `db.get_species_list()`
   - `POST /api/measure`：呼叫 `pipeline.py` 的入口函式，回傳 JSON（前端 `app.js` 完全不用改，欄位名稱已經對好了）
4. 等 Azure SQL 連線恢復後：`Species_Ref` 加欄位 + 匯入樹種資料
5. 實測：上傳一張真實照片跑完整流程

---

## 7. 額外備註

- `services/measure/geometry.py` 裡的 `DBH_HEIGHT_M`／`DBH_SLICE_COUNT`／`MIN_VALID_SLICES` 是直接寫死在檔案裡的常數（原本 Tree-Trunk-Measurement 是從它自己的 `config.py` 讀），因為沒有把那份 config.py 搬過來，所以改成模組內常數，邏輯數值完全沒變。
- 兩個模型檔（`Tree-Trunk-Segmentation/best.pt` vs Tree-Trunk-Measurement 自己的 `best.pt`）是不是同一份訓練結果，**還沒確認過**，目前決定共用主站現有的那份。
- 這個功能所在的分支 `simple_measurement` 已經合併進 `main`（PR #33），現在直接在 `main` 上繼續開發即可，不用再開新分支（除非要另外開一個新功能分支）。
